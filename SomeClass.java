@SuppressWarnings("ALL")
public class SomeClass {
    // visibilidade pública (apenas uma por arquivo) também chamada de top level class,
    // ou seja, definida diretamente dentro do arquivo .java que leva o mesmo nome da classe
}

// public class D{} ERRO -> Class 'D' is public, should be declared in a file named 'D.java'

